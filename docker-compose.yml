services:
  monitor:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - monitor-db:/app/db
      - monitor-logs:/app/logs
    restart: unless-stopped

volumes:
  monitor-db:
  monitor-logs:
